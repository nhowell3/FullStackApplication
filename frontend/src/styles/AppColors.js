// Light Mode Colors
const backgroundL = '#f0f0f0';
const backgroundDarkerL = '#dbdbdb';
const textL = 'black'
const accentL = 'blue';
const placeholderL = 'grey';

// Dark Mode Colors
const background = '#1f232e';
const backgroundDarker = '#151821';
const text = '#DFDFDF';
const accent = 'orange';
const placeholder = 'grey';

const LightModeColors = {
    background: backgroundL,
    backgroundDarker: backgroundDarkerL,
    text: textL,
    accent: accentL,
    placeholder: placeholderL,
}

const DarkModeColors = {
    background: background,
    backgroundDarker: backgroundDarker,
    text: text,
    accent: accent,
    placeholder: placeholder,
}

const AppColors = DarkModeColors;

export default AppColors;
